<form method="post" action="{{ path('app_departementDomaine_delete', {'id': departementDomaine.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ departementDomaine.id) }}">
    <button class="btn btn-sm btn-danger btn-delete">supprimer</button>
</form>
